services:
  web:
    build:
      context: .
      dockerfile: Dockerfile
      args:
        PUBLIC_GA_MEASUREMENT_ID: ${PUBLIC_GA_MEASUREMENT_ID:-}
    container_name: blog
    restart: unless-stopped
    ports:
      - "127.0.0.1:8085:80"
